---
import Hero from "./hero.svelte";
import ScrollingIcons from "./scrolling_icons.svelte";
import ScrollingGames from "./scrolling_games.svelte";
import Footer from "./footer.astro";
---

<section class="hero">
	<Hero
		style="height: 100vh; width: 100vw; overflow-y: auto;"
		client:visible
	/>
	<div class="fade">
		<style>
			.fade {
				position: absolute;
				top: 1px;
				left: 0;
				width: 100%;
				height: 100%;
				background: linear-gradient(
					to bottom,
					rgba(0, 0, 0, 0) 0%,
					rgba(0, 0, 0, 0.1) 90%,
					rgba(0, 0, 0, 1) 100%
				);
			}
		</style>
	</div>
</section>
<ScrollingIcons client:visible />
<section class="game-carousel">
	<a
		href="#games"
		id="games"
		aria-label="Scroll to games section"
		style="color: white;"
	>
		<h2>Nuestros Juegos</h2>
	</a>
	<p>
		Selecciona entre nuesta variedad de juegos, pasa el mouse por encima
		para revelar más informacion.
	</p>
	<div class="game-container">
		<ScrollingGames direction="up" styles="width: 100%;" client:visible />
		<ScrollingGames direction="down" styles="width: 100%;" client:visible />
		<ScrollingGames direction="up" styles="width: 100%;" client:visible />
		<ScrollingGames direction="down" styles="width: 100%;" client:visible />
	</div>
	<style>
		.game-container {
			display: flex;
			flex-direction: row;
			align-items: center;
			justify-content: center;
			width: 100%;
			height: 100%;
			overflow: hidden;
		}
	</style>
</section>
<Footer />